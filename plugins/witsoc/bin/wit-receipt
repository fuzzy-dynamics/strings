#!/usr/bin/env bash
# Pipe verifier output (stdin) into `wit receipt` to persist verdicts.
set -euo pipefail
PLUGIN_DIR="$(cd "$(dirname "$0")/.." && pwd)"
"$PLUGIN_DIR/bin/ensure-deps" >/dev/null 2>&1 || "$PLUGIN_DIR/bin/ensure-deps"
exec "$PLUGIN_DIR/data/venv/bin/wit" receipt "$@"
